import { Jost, Roboto } from 'next/font/google';

export const jost = Jost({
  subsets: ['latin'],
  weight: ['400', '600', '700'],
});

export const roboto = Roboto({
  subsets: ['latin'],
  weight: ['400', '500', '700'],
});
